<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\LuxMap.Shared\LuxMap.Shared.csproj" />
    <ProjectReference Include="..\LuxMap.Persistence\LuxMap.Persistence.csproj" />
    <!-- A fault is observed AT a pole, on a fixture, along a segment. Real foreign keys, declared
         WITHOUT navigation properties — the same convention as HasCommuneReference: full integrity
         in the database, coupling in the object model kept at the level of an id string. -->
    <ProjectReference Include="..\LuxMap.Modules.Assets\LuxMap.Modules.Assets.csproj" />
    <!-- reported_by / confirmed_by / resolved_by are real foreign keys to app_user. -->
    <ProjectReference Include="..\LuxMap.Modules.Identity\LuxMap.Modules.Identity.csproj" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

</Project>
